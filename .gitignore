# Temprary files
OLD-frontend

# Node modules (dependencies)
node_modules/
npm-debug.log
yarn.lock
pnpm-lock.yaml
*package-lock.json

# Dependencies and lock files
yarn.lock
pnpm-lock.yaml

# env file
.env